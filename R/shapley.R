#This function takes in a response vector, a design matrix, and calculates the
#shapley value for each column of the design matrix using either AIC or R^2.
shapley <- function(Y,X,score="AIC"){
  return(NULL)
}
